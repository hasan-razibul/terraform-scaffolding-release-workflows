name: "Fetch release asset"
on: workflow_dispatch

jobs:
  asset_fetcher:
    runs-on: ubuntu-latest
    steps:
    - uses: dsaltares/fetch-gh-release-asset@master
      with:
        version: 'latest'
        file: "scaffolding_.*\\.zip"
        token: ${{ secrets.GITHUB_TOKEN }}
